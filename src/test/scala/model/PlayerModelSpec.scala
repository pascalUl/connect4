package model

import org.scalatest.wordspec.AnyWordSpec

class PlayerModelSpec extends AnyWordSpec {
  "A PlayerModelSpec" should {
    "have a name" in {}
    "have a symbol" in {}
    "have a string representation that equals its symbol" in {}
  }
}
